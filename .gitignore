# コンパイルされたバイナリファイル（Windows用、Mac/Linux用）
*.exe
*.exe~
*.dll
*.so
*.dylib

# 開発中に生成されるバイナリ（プロジェクト名と同名のファイルなど）
/go-sync-project

# ツールや環境ごとの一時ファイル
.DS_Store
.vscode/

# スマホ/ブラウザ確認用のWASMビルド出力（build_wasm.shで生成、都度作り直す）
/web/game.wasm
/web/wasm_exec.js

# Claude Code のローカル設定（個人環境ごとの設定なので共有しない）
.claude/